/**
 * Keep developer mutation tools out of TestFlight/public-style builds.
 * Flip this only in local/internal development when those tools are needed.
 */
export const SHOW_DEVELOPER_TOOLS = false;
